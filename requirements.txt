fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-multipart==0.0.12
httpx==0.27.2
tenacity==9.0.0
structlog==24.4.0
prometheus-client==0.21.0

# Vector DB + embeddings
# Chroma 0.5.7 was built against numpy<2, onnxruntime<1.20, tokenizers<0.21.
# Pin the transitive wheels to that era so pip's resolver stops trying to
# pull the latest and silently OOM the build.
chromadb==0.5.7
# Embeddings use ChromaDB's bundled ONNX MiniLM embedder (all-MiniLM-L6-v2,
# 384-d) so we don't need to install torch-backed sentence-transformers.
numpy<2.0
onnxruntime>=1.14.1,<1.20
tokenizers<0.21

# BM25 retrieval + reranking
rank-bm25==0.2.2

# Document parsing
docling==2.18.0
rapidocr-onnxruntime==1.4.4
pdfplumber==0.11.4

# LLM client (OpenAI-compatible for Ollama Cloud)
openai==1.55.0

# Memory store
motor==3.6.0
pymongo==4.9.2

# UI
streamlit==1.40.0

# Observability (optional, gated by env)
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0

# Rate limit + injection defense
slowapi==0.1.9
limits==3.13.0

# Tests
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-httpx==0.32.0
respx==0.21.1
